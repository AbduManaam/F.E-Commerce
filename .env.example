# Frontend API URL (build-time; must match backend URL reachable from browser)
# Local dev: backend runs on localhost:8080
# Docker: use backend container hostname or public URL (e.g. http://localhost:8080 if port-mapped)
VITE_API_URL=http://localhost:8080
